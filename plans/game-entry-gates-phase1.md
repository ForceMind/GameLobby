# 第一期游戏准入规则

- 目标：按 `client/src/pages/DocsPage.jsx`「准入规则与进入门槛」及交接要求，落实游戏进入门槛和运营标签。
- 基线：`lobby-admin-lite-v1`，起始提交 `6391437`，版本 `0.3.1`；81 项测试通过。未跟踪交接文件保留，不纳入提交。
- 授权：第一期实现、必要修正、测试、真实浏览器验证、文档和本地分批提交；不推送、不部署。第二、三期保持未实现。
- 约束：地区不匹配隐藏；门槛不满足保留卡片并说明原因；宿主缺字段不放行；配置沿用草稿、审核及回滚，运行状态/维护公告保留紧急生效机制；玩家文案只补中英文；响应式使用 `@container lobby`。
- 既有原型边界：后台配置存于 AdminApp 内存，玩家目录读取静态数据；共用示例字段，不新增后台与玩家页的持久化或服务同步。

## 阶段与所有权

1. 只读规划：gpt-5.6-sol / ultra，核对宿主数据、前台状态与风险；不写文件。
2. 规则与输入：gpt-5.6-terra / high，负责 `data.js`、`demoModel.js/test.js`、`h5/hostContext.js/test.js`。判定接口 `gameGate(game, { account, wallet })`，返回 `{ ok, reasons: [{ key, need, have }] }`；缺失 have 为 null。示例 Golden Pharaoh 财富 5，Fish Hunter 家族专属。
3. 后台配置与审核：gpt-5.6-terra / high，负责 `admin/AdminApp.jsx`、`adminSchema.js`、`adminRules.js/test.js`、必要的 `admin.css`；示例从 `data.js` 读取，未配字段取约定默认值。
4. 主控集成：H5Provider、游戏卡片/说明弹窗、玩家文案和样式、版本与文档。各代理共享工作区、互不覆盖，测试串行运行，避免重复启动浏览器或构建。
   - 浏览器复现后的必要修正：core 额外负责 `engagement/service.js` 与服务测试；真实宿主仅有 context 时也不得启用预览余额。`level/coins` 显式非法值改为清除旧资格，原断言按更严格语义更新，未删除测试。
   - 开发验收入口：`client/qa/game-gates.html/jsx`，仅本地开发使用，未加入构建入口；用于宿主事件、旧回调、直接启动及全门槛原因的复验。
5. 独立审查与验收：审查者只读核查关键实现；主控修正并按风险定向回归。

## 验收与恢复

- 纯函数覆盖全部门槛、等于阈值、未知/非法玩家值与不限；宿主边界覆盖合法/非法/部分更新及身份切换。
- 审核差异显示所有新增字段，性别/布尔可读；草稿隔离、审核通过及回滚得到验证。
- 实际打开并操作后台和玩家页；验证财富锁定、家族放行、绕过 UI 的启动拒绝、国家切换隐藏、弹窗打开后上下文变化。
- 375px 和 1440px 五页实际渲染及主要导航，检查溢出、遮挡、版本；检查中英文。
- lint、全部测试、build 通过，记录测试数和浏览器证据；可按提交逆向回滚本次增量，保留原基线。
- 新增重要范围或外部副作用需补充决定；资源/权限受限时保留成果并记录缺口。

## 进度

- 已完成：需求核对；全部一期字段、准入判定、宿主清理、玩家锁定/弹窗/最终拦截、后台草稿/审核/回滚、版本和接入文档。
- 独立审查：gpt-5.6-sol / ultra 只读审查核心与后台；发现的演示余额旁路、旧等级/余额资格及紧急状态被旧快照覆盖均修复并复核通过。
- 浏览器：已实际验证七字段审核/回滚、紧急状态交错、六门槛/旧启动回调/字段撤销/地区切换，以及375px、1440px五页；详细步骤与实测值见 `client/QA.md`。
- 质量检查：最终 `npm run lint && npm test && npm run build && npm run check:dist` 全部退出 0；97 / 97 项测试通过（基线 81，新增 16）。最终构建入口 `start-v0.3.2-aff142a9edf57080.html`，八个入口/资源版本/404/no-store 均验证；开发 QA 入口不进入产物。`git diff --check` 通过。
- 本地提交：`667ba46`（规则与可信宿主输入）、`bf11049`（后台治理与紧急状态保护）；玩家界面、版本和本记录随第三批 `feat: explain locked games and recheck every launch` 提交。
- 验收状态：已完成。三个提交均保留在本地；没有 push 或部署。未跟踪的 `HANDOFF-游戏准入门槛.md` 保持原状。
