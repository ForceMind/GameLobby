# 多语言管理与翻译往返修正（2026-09-09）

用户已批准上一轮诊断提出的修正。当前阶段：已完成（本地交付，未部署）。

## 范围与交付

- 每种语言只出现一次，简中原文、英文参考/兜底、目标译文职责明确。
- 翻译CSV包含稳定语言编码和基准快照；严格校验文件结构、语言、重复键、占位符、原文及译文冲突。支持带引号、逗号和多行文本。
- 空白默认跳过，明确操作才清空；错误整批阻断。逐条预览并在确认时重查，更新目标语言。
- 原文或英文改变后已有相关译文进入待复核，可筛选、导出、编辑或明确确认；未复核不能提交。
- 原型仍是页面内存数据，准确说明刷新重置、模拟审核与玩家静态语言包边界。真实后端、部署、发布及远端同步不在本批范围。
- 页面版本 0.3.5 → 0.3.6，更新操作说明和变更记录。

## 执行与所有权

沿用一个持久Goal。主智能体负责 AdminApp、TranslationEditDialog、样式、schema/rules接入、版本、文档及浏览器集成验证。
translation_transfer（gpt-5.6-terra/high）独占 translationTransfer.js 与测试；translation_review（gpt-5.6-terra/medium）独占 translationReview.js 与测试。
最多三个并行执行者，纯逻辑测试轻量本地运行；集成后 lint、test、build、check:dist 各一次，出现新问题仅针对性回归。
保留既有未跟踪 HANDOFF 文件。完成后安排独立只读审查。

## 验收

- 简中、英文及第三语言表头无重复，编辑默认定位所选语言。
- CSV往返、多行、空白/清空、错语言、未知表头/键、重复键、占位符、过期文件、预览后变更有行为验证。
- 原文修改→译文待复核→确认→审核的状态及快照一致。
- 浏览器实际检查相关页面、导入预览、取消/错误阻断及成功反馈，桌面和窄屏布局。
- 真实玩家发布仅记录未接入，不以构建/模拟审核替代生产证据。

## 进度与证据

- 已核对当前代码与Git状态；基础提交 985b005。
- 实现已完成：重复语言列去重、当前语言编辑、v2 CSV严格解析/冲突检测、明确清空、分页预览、复核元数据与审核快照。
- 独立只读审查：translation_audit（gpt-5.6-sol/high），最终通过。发现的原文可清空、高行数导入资源风险、公式形态文本导出、模拟任务误计生产数量均修复；集成时误扩散的状态表达式已收敛并回归玩家列表。
- 最终 `npm run verify` 退出0：lint通过，117/117测试通过，build通过，check:dist通过。产物入口 `start-v0.3.6-e30875f30b9c8712.html`；`git diff --check`通过。
- 浏览器：Codex内置浏览器，开发地址127.0.0.1:5175/admin.html，桌面约1280×720及390×844。验证简中/英文/日语列唯一、CSV下载反馈、错误语言拒绝、多行文件预览/确认写入、旧文件冲突拒绝、原文改变→英文/日语待复核→逐项确认→模拟审核完成、原文清空拒绝、取消保留原值、窄屏按钮/弹窗布局、玩家列表正常和错误日志为空。
- 浏览器调用期间文件选择返回曾异常缓慢；最终返回均已核对。没有使用源码/纯函数测试代替上述交互结果。
- CSV公式形态保护、LF/CRLF/单独CR逐字往返有纯函数测试与独立复核；未在Excel/Numbers/LibreOffice实际打开重存。以可逆文本标记保护导出，标准CSV回传校验失败时不写草稿。
- 版本0.3.5→0.3.6；操作说明在docs/translation-workflow.md，README/CHANGELOG/QA同步。本批不提交、不推送、不部署；既有HANDOFF文件保留。
- Goal完成。真实服务端持久化、权限审核、玩家语言包分发仍属后续独立工作。

## 仓库同步与部署（用户后续授权）

2026-09-09 用户明确要求「更新仓库、部署」。本次沿用 origin/lobby-admin-lite-v1，推送本批代码与文档；向已有 joyloop Pages 项目的同名分支预览部署，保留 main 生产环境。部署后核对远端提交、Pages记录、实际资源哈希/MIME/no-store/404和浏览器版本。状态：进行中。
