# Joyloop 静态原型评审与交付边界

## 评审输入

初版 `screens.zip` 与修订版 `joyloop.zip` 均为 HTML 参考文件，未附带其引用的完整 CSS、JS、SVG 与游戏封面资源。因此本仓库采用自包含 CSS、图标和代码原生插画重建，不能宣称逐像素复刻最终美术。

## v0.2.0 吸收内容

- 大厅先经过说明与同意入口；默认半屏 1:1，边长为 `min(viewportWidth, viewportHeight, 640px)`，可切换全屏。
- 可进入游戏先显示全屏加载；加载中左上角可用 `Lobby` 返回，进入游戏后只保留游戏右上角关闭按钮，关闭后恢复进入前模式。
- 金币、宝石余额和导航可点击；个人页补充账号信息、复制账号 ID、等级和成就摘要。页面不提供独立退出登录，身份由内置 App 管理。
- 商城统一美元口径：`1 USD = 10,000 金币`；礼包按固定金币数量与折扣价购买，不按折扣增加金币。
- 清理用户界面的开发说明和旧货币文案；技术限制只保留在维护文档和宿主接入契约中。

## 当前静态配置

| SKU        |    金币 | 折扣 |   原价 | 折后价 |
| ---------- | ------: | ---: | -----: | -----: |
| `coin-6`   |   6,000 |   8% |  $0.60 |  $0.55 |
| `coin-30`  |  30,000 |  18% |  $3.00 |  $2.46 |
| `coin-68`  |  68,000 |  28% |  $6.80 |  $4.90 |
| `coin-128` | 128,000 |  40% | $12.80 |  $7.68 |

以上折扣率沿用当前静态配置，属于业务最终确认前的待定价格；不应由前端单独作为真实报价来源。宝石赠礼是独立配置，不改变金币数量。

## 当前交付与边界

项目提供七个静态 HTML 入口：大厅、全部游戏、赛事、活动、商城、个人中心及根入口别名。筛选、导航、弹窗、表单、签到、转盘、任务、报名/候补、兑换码、商城确认和个人设置均有本地反馈。余额、订单、奖励记录、赛事结算和设置不是服务端持久化事实。

本地可交互游戏仅覆盖加载、进入、关闭以及少量局部操作（转轴或气泡）。它验证页面流程，不代表真实游戏引擎、实时对局、扣款或资产结算已经接入。

## 接入边界

`window.JoyloopHost` 可注入公共账号、钱包和请求方法。页面只发送版本化 `requestId/action/payload` 请求；宿主负责真实容器尺寸、App 账号、支付授权、服务端重价、到账确认与幂等处理。页面不会传递 token，也不会在桥接不可用时伪造成功。完整契约见 [HOST-INTEGRATION.md](../client/HOST-INTEGRATION.md)。

默认响应头为 `SAMEORIGIN`，顶层原生 WebView 和同源 iframe 可加载；跨源 iframe 需要另行确认允许域名、CSP 和宿主策略。

## 尚未实现

- 正式支付、订单、退款、资产账本、风控和服务端到账
- 实际游戏引擎、实时对局、赛事服务端规则与结算
- App 登录/注销、服务端身份持久化和跨设备同步
- 运营配置、生产级加载资源、权限控制、错误恢复和数据分页
- 原型缺失资源的最终视觉还原

## 验收入口

代码级检查、交互契约和浏览器证据维护在 [client/QA.md](../client/QA.md)；部署步骤在 [client/DEPLOY-CF-PAGES.md](../client/DEPLOY-CF-PAGES.md)。本页不重复声明某次浏览器运行的组数或提交哈希，避免把一次本地检查误解为线上验证。

## 待产品确认

- 当前四档折扣率、月卡定价、续费、漏领补发与退款规则
- 漏签、补签、连续奖励和活动结算规则
- 赛事计分、平分顺序、候补扣费、退款和报名锁定规则
- 真实游戏启动参数、权限、加载失败恢复和关闭后的状态同步
- 目标宿主域名、跨源 iframe 是否需要支持，以及对应 CSP/安全策略
