# v0.3.3 手机预览与默认语言修复

验收日期：2026-09-08；基线 `f38ff60` / v0.3.2。用户反馈四项问题均已复现并修复，版本更新为 v0.3.3。

已发布源码 `4c302eb`，唯一部署 `https://472bcf1e.joyloop.pages.dev`，当前分支新入口为 `start-v0.3.3-7c1915c7e8589465.html`。线上再次实测默认中文、两档实际尺寸、16px圆角、加载/游玩均在设备框内及关闭恢复比例；17文件哈希、no-store与404通过。发布记录见 `DEPLOY-CF-PAGES.md`。

| 场景 | 修复前 / 验证结果 |
| --- | --- |
| 首页语言 | 原先跟随浏览器/历史语言。实际先在玩家页保存英文，再打开无lang的index：首页仍为简体中文；显式lang=en仍英文；进入大厅链接携带lang=zh-Hans |
| 1440×800 比例 | 修复前18:9和21:9均为430×736；修复后分别368×736、315.42×736，选择21:9实际变窄 |
| 1366×768 比例 | 21:9为301.70×704，实际宽高比0.42856；页头及主内容没有横向溢出 |
| 游戏加载/游玩/关闭 | 修复前game父项为BODY、宽1440；现在为h5-lobby直接子项，加载与游玩边界与315.42×736设备框一致。气泡实际点击后得分20、再来一局可重置，关闭后比例及尺寸保持 |
| 小圆角和安全区 | 桌面圆角44→16px，为通用示意，不对应某款手机规范。页头padding为12px 8px 0，底栏padding为4px 8px 16px；游戏关闭按钮距离顶部24px、右侧12px |
| 桌面短窗口 | 1440×560、21:9为212.56×496；页头可换行、游戏内容宽与scrollWidth一致，关闭按钮仍处于设备框内 |
| 半屏恢复 | 1440×800时640×448半屏进入游戏后视觉容器full，关闭恢复640×448。开发验收入口记录half/lobby→full/game→half/return-to-lobby，entryMode一直为half；拒绝财富门槛时没有新增启动请求 |
| 手机视口 | 375×812时设备/游戏为375×812、圆角0、页头人为padding0，比例开关隐藏；未将桌面安全区强加到手机 |
| 横屏滚动 | 844×390时游戏内容scrollHeight528、滚至138后关闭按钮仍距顶12px且可点击；关闭恢复half |
| 五页回归 | 375px下大厅/游戏/活动/商城/我的通过真实导航打开，主内容宽和scrollWidth均375、页头均359；代表页面截图检查通过 |

`npm run verify` 全绿：lint、98/98测试、build、check:dist。新增首页语言行为测试，原有97项保留；宿主模式队列、游戏准入及审核等原有检查继续通过。Sol/ultra独立只读审查无阻断问题。测试使用实际浏览器交互和DOM尺寸，不将截图当作游戏操作证据；手机为浏览器视口模拟，未测试硬件系统安全区或真实原生App回调。开发入口记录的是公开桥接请求事件，未伪造原生成功响应。

---

# v0.3.2 游戏进入门槛验收

验收日期：2026-09-08。基线 `6391437` / v0.3.1；本期版本 v0.3.2。后台原独立显示标签 v0.6.0 已改用统一构建版本。范围为本地静态原型，不含部署、远端同步、真实宿主服务或第二、三期。

最终质量检查：`npm run lint && npm test && npm run build && npm run check:dist` 全部通过，97 / 97 项测试（原 81，新增 16）。独立只读审查已通过。

### 后续 Pages 发布验收 · 2026-09-08

用户后续授权部署及仓库同步。v0.3.2 / `26815bd` 已发布至 `lobby-admin-lite-v1.joyloop.pages.dev`，唯一部署为 `72e250f9.joyloop.pages.dev`，未切换 main 生产环境。线上实际点击验证财富锁定弹窗的开始按钮禁用，Fish Hunter 可进入；后台进入门槛/家族专属两列、财富5示例及文档“已实现 · v0.3.2”均已回读。详见 [部署记录](DEPLOY-CF-PAGES.md)。

17 个线上可访问文件与包内 SHA-256 一致，`no-store` 与脚本/样式 MIME 校验通过，缺失脚本返回404；本地证据为 `artifacts/pages-v0.3.2-online-verification.json`。只更新仓库 Markdown 后沿用原有代码测试结论，不重复构建新随机入口。

## 规则与审核

- 四个数值门槛、性别、家族归属分别失败、全部满足、阈值相等、未配置不限及玩家缺字段都有行为测试。新增运营标签不参与准入。
- 同账号缺省增量保留；准入字段显式无效时清除旧资格；`familyId: null` 退出家族；合法账号 ID 改变或首次绑定时隔离旧账号/钱包。原非法等级与余额测试按更严格的准入契约加强，未删除测试。
- context-only 宿主也使用服务端适配器，离线时不回退预览余额。浏览器曾复现缺失余额先为 0、后被活动预览改成 52,860；修复后缺字段及接口失败一直保持安全 0。
- 真实后台操作了全部七字段：财富 5→6、魅力 0→4、余额 0→1000、可玩等级 0→12、男/女→男、家族否→是、Hot→New。保存后待审核；差异为 7 / 217 项，性别、布尔、运营标签可读。通过后与生效版 0 差异；回滚后恢复原七字段。
- 后台负数及性别全部取消时保存禁用；字符串数字、布尔、数组与非有限数的门槛在审核阶段也被拒绝。单游戏差异为 1 + 27 = 28 行，八游戏为 1 + 8×27 = 217 行，未改动原转盘的 11 行断言。
- 独立审查发现旧快照可能覆盖紧急状态，已修复并实际复验：财富草稿待审后切维护，通过后仍维护且财富为 6；随后切暂不可用并更新公告，回滚后财富恢复 5，暂不可用和最新公告保持。灰度同一规则有单测覆盖。

## 真实浏览器操作

使用 Codex 内置浏览器打开本机 Vite 页面并实际点击；视口按 DOM 实测核对。375px 为浏览器设备尺寸模拟，未使用手机硬件。

| 项目 | 实测结果 |
| --- | --- |
| Golden Pharaoh 演示财富 3 / 门槛 5 | 主目录、最近在玩保留卡片并显示锁和差值；弹窗开始按钮禁用 |
| Fish Hunter 家族专属 | 演示账号有家族，点击开始进入真实原型加载状态 |
| 最终启动拦截 | 开发入口直接调用保存的旧 openGame，甚至传入 wealthLevel:0 / familyOnly:false，仍按目录真实配置拒绝；无游戏展示、无启动请求 |
| 弹窗期间上下文变化 | 财富达到 5 后按钮可点，降低后立即禁用；清除家族并同一步调用旧回调被拒绝 |
| 全部门槛 | `host=missing&gates=all` 列出六种未满足原因；补齐全部字段且恰好等于门槛可进入；撤销等级/余额再次禁用与拒绝 |
| 地区 | CN 时 Ocean 777 在目录显示，切 US 后消失；财富锁定游戏仍保留 |
| 375×812 | 大厅、游戏、活动、商城、我的分别通过底部导航打开并检查截图；页面/内容宽与 scrollWidth 均为 375，无横向溢出 |
| 1440×1000 | 五页保留居中模拟手机框，内容宽与 scrollWidth 均为 430；页面宽为 1440 |
| 中英文 | 中文和英文的 375px 锁定卡片、原因及禁用按钮检查通过；英文弹窗宽与 scrollWidth 均为 353 |
| 可见版本 | 玩家、入口、文档共用 version.js；后台改为同源引用，显示 v0.3.2 |

开发复验：启动 `npm run dev` 后打开 `/qa/game-gates.html`，使用表单输入 `joyloop:context` 数据；`?host=missing` 为缺字段宿主，`?host=missing&gates=all&lang=en` 为全门槛场景。按钮调用公开 H5Provider API，不读取 React 私有状态。该入口不在生产构建输入中。

后台与玩家页没有跨页面发布同步，后台内存状态刷新重置；本期验证的是共享静态种子、玩家准入行为与后台草稿/生效版本治理各自的完整流程。国家/身份的 null 值不构成注销协议，见 HOST-INTEGRATION.md。完整质量检查及提交记录见 `plans/game-entry-gates-phase1.md`。

---

# v0.3.1 静态资源、中奖列表、直播入口与部署包验收

验收日期：2026-09-01。范围为本地生产构建、九个独立页面（含运营后台原型）、简体中文与英文。

## v0.3.1 运营后台原型验收

- 首页可打开 `admin.html`，后台可返回首页；Vite MPA 构建和 Cloudflare Pages 打包均包含后台入口。
- 仪表盘展示用户、在线、游戏状态、业务摘要、待办和最近发布卡片。
- 所有侧栏菜单均可进入有内容的列表页；签到、转盘、任务、家族、中奖、赛事结算、商城、钱包、兑换码、客服、分析、风控和系统设置不再是空页面。
- 列表页支持关键词搜索、状态筛选、详情抽屉和新建配置弹窗；详情包含状态流转、最近操作和提交审核动作。
- 390px 窄屏隐藏侧栏并显示移动菜单按钮，页面无横向溢出；桌面端维持截图参考的浅色卡片式信息密度。
- 后台数据为 Mock 原型，不代表真实玩家、资产、订单、概率或结算事实。
- 后台操作语义：仪表盘、待办、日志和订单等只读/处理页面不显示无关的“新建”或常驻“发布中心”按钮；创建、上传、发布和导出动作按页面职责显示。
- 游戏版本链路：游戏版本、上传记录、测试环境和生产环境分栏展示；版本弹窗支持文件选择、自动检查、发布说明和保存发布任务。
- 环境切换：顶部可在测试/生产环境间切换，游戏目录和版本工作流会同步显示当前环境及对应风险提示。
- 游戏目录排序：游戏列表提供表格/卡片视图，行和卡片带 `draggable` 能力；拖拽后出现未保存状态，保存排序草稿后再进入发布流程。
- 后台用户：后台用户和玩家管理分离，用户列表展示角色、账号状态、环境权限和最近登录；角色页展示菜单范围和数据范围。

## v0.3.1 本轮功能验收

- 赢家榜个人状态：公开榜单和完整榜单弹窗底部均显示当前用户排名、金币和距前十差值。
- 最新中奖列表：活动页末尾展示 5 条记录，含头像、昵称、时间、游戏、金币、好友同玩人数和“我也要玩”。
- 直播快速入口：大厅最近在玩下方展示最多 3 个具体房间，按钮调用统一直播桥，不依赖 Banner 才能进入。

- 品牌版本号：入口页和业务页的 Joyloop 品牌下方均显示 `v0.3.0`，窄屏不隐藏。

- Hot Live Rooms：GamesPage 展示专区，支持四类筛选、直播中优先排序、房间预览和不可用状态。
- 直播 Banner：大厅增加“正在直播”卡片，保留自动轮播、暂停、鼠标拖拽和触摸左右滑动；CTA 保留 `from=game_center&entry=banner`。
- 独立直播入口：大厅“最近在玩”下方显示“正在直播”入口卡，完整专区不依赖 Banner 才能发现；直播 Banner 仅推荐单个房间。
- 游戏层入口：可按当前游戏匹配直播房间，按钮仅在有可用房间时启用；点击后显示预览状态或调用宿主桥。
- 归因参数：房间入口携带 `from`、`entry`、`room_id`、`game_id`、`mode` 和 `lang`；无宿主时不伪造真实进房。

- PC 全屏大厅：热门游戏与今日赢家榜在同一行且顶部基线一致；DOM 实测两者 top 差值小于 2px。家族 / 派对活动模块不出现在大厅游戏列表中，位于活动页总览下方。
- 全局获胜弹幕：屏幕内容层上方约 30% 区域最多同时展示四行“昵称、游戏、赢得金币”，按随机间隔投放，当前行未结束才占用下一行，满四行后排队；每条弹幕后有“我也要玩”入口并直接打开对应游戏；大厅、目录、赛事、活动、商城和个人页均渲染。
- 语言选择：原型入口与个人设置均使用自绘下拉菜单，页面不渲染原生 `<select>`。
- 商城活动卡：购买保障条目已移除，破产保险箱需先购买；未购买时展示预计可恢复金币和恢复排行榜，购买后整卡变为已生效/待结算，详细规则和每日刷新时间集中在右上角设置弹窗，购买后可选择一次并锁定至下月。
- 商城保险箱状态：未购买时展示完整功能说明、预计可恢复金币和始终可见的恢复排行榜；购买后整卡切换为已生效/待结算，右上角设置按钮启用。
- 大厅精选活动：Banner 支持鼠标拖拽和触摸左右滑动，滑动超过阈值切换前后卡片，点击链接或控制按钮不会误触发切换。
- 隐私设置：个人页设置包含接收他人获胜通知、允许发送我的获胜通知、向好友展示最近玩的游戏三个 switch；关闭接收后弹幕隐藏，关闭发送后不展示当前用户记录，关闭最近游戏展示后大厅标识变为“仅自己可见”。
- 响应式：移动端弹幕轨道高度 36px；半屏保持游戏优先布局。
- 直播范围：本轮覆盖 Hot Live Rooms、直播 Banner、游戏层找房间入口和归因参数；Game Revenue Share 仍未接入真实结算。
- 当前为静态原型状态，隐私设置不持久化，获胜事件不连接推送服务。

## 当前交互契约

| 场景          | 行为                                                                           |
| ------------- | ------------------------------------------------------------------------------ |
| 原型说明首页  | 面向评审者解释页面范围、交互与预设数据；默认全屏，无勾选或确认，点击直接进入   |
| 半屏大厅/目录 | 游戏优先；保留最近、热门、全部游戏、筛选与底部入口，隐藏排行榜、横幅和大快捷卡 |
| 半屏个人页    | 显示资料、编辑入口、本周统计和资产卡；成就、战绩、安全设置等保留更多入口       |
| 半屏商城      | 显示四档真实礼包配置与购买确认；月卡、兑换码保留紧凑入口                       |
| 半屏活动      | 可领取今日签到和已完成任务、抽取转盘、查看奖励记录；展示任务简表               |
| 半屏赛事      | 显示三场赛事摘要、奖池、报名费和人数，可看规则、报名或加入候补                 |
| 更多内容      | 仅提示从全屏入口打开，不改变当前 URL 或布局，不用空提示页替代半屏内容          |
| 资产浮窗      | 显示完整余额与最近变动；获得进入当前布局的活动页，充值进入当前布局的商城       |
| 游戏          | 按既定流程临时全屏加载/运行，游戏右上关闭后恢复原页与原布局                    |

首页与业务页分开。页面内导航保持当前模式，不能把半屏直接改成全屏；只有从独立入口选择模式才能进入不同布局。业务页不读取旧布局缓存，不增加全局同意门槛。

## v0.2.4 本轮检查

- 已确认的用户报错：模块请求收到 `text/html`，不是已确认的 JavaScript API 兼容错误。线上当前脚本可正常返回，部分不存在的脚本路径却返回首页 200 HTML 并带长期缓存。尚未取得用户失败请求的准确 URL，不能把旧缓存认定为唯一原因。
- 修正：所有静态响应配置 `no-store`，JS/CSS 使用版本目录，提供全新说明入口，根级 404 禁止缺失资源回退首页。不修改产品页面设计、业务交互或强行改变响应 MIME。
- `check:dist` 检查所有八个页面引用的 JS/CSS 存在且位于当前版本目录，资产内容不是 HTML；新入口必须逐字等于 index；404 不依赖 JS；响应头不得保留一年 immutable 规则。
- 打包检查包含新入口与 404，逐文件比对解压后的 SHA-256，不允许遗留测试文件混入。
- `npm run verify` 通过：42 项测试、生产构建和增强后的资源检查。本地 MPA 预览的八个页面及新入口返回 200，新版 JS/CSS 返回正确类型，成功响应带 no-store，不存在的 JS 返回 404。Vite 预览不解释 Pages 的 `_headers`，其 404 不带预览自定义头；最终 Pages 响应头仍需部署后按指南检查。
- 浏览器实测新入口默认 full 可进入个人页；选择 half 后，金币浮窗充值进入半屏商城，四档礼包和返回入口保留。
- 尚未替用户上传本版或验证其手机；上线验收步骤见 [部署指南](DEPLOY-CF-PAGES.md)。

## v0.2.3 已有验收记录

- `npm run verify`：ESLint、35 项自动测试、生产构建和七个静态入口检查通过。
- 新增返回规则、内部导航、修饰键/下载/外链放行、点击阻止文档导航、历史前进后退、无 mode 参数的全屏历史入口及事件清理测试。
- 浏览器实点 360px 宽中英文 half/full 的全部六个业务页，共 24 组：返回始终显示，各模式的顶部栏位置与高度一致，内容无横向溢出，英文正文无残留中文。390px 半屏连续切页同样保持外框和内容区尺寸。
- 半屏金币浮窗的充值实点到半屏商城，浮窗关闭；不是跳回首页或改为全屏。
- 英文完整个人页只显示异常登录检测，详情可打开；语言从英文切到中文后，返回大厅链接仍保持当前语言和 full。
- 大厅无宿主时的左上角退出可返回独立说明首页，默认 full，无勾选步骤。
- 1366px 英文桌面个人页返回大厅，Logo、顶部栏与资产栏的位置不变。游戏加载左上 Lobby 可见，进入游戏后只保留右上关闭；关闭恢复 half 与原大厅。
- 已删除手机号绑定、登录保护、支付密码及自动旋转的界面、配置与对应翻译。安全检测仍为预设状态，未连接真实安全服务。
- 业务内容仍保留原有按页的临时交互状态模型；本轮未将其改为持久化账户数据。页面刷新会重置预设交互。

## v0.2.2 已有验收记录

- `npm run verify`：ESLint、29 项自动测试、生产构建、七个 HTML 入口与资源检查通过。
- 共 48 组页面/语言/布局检查通过：390×844 的中英文半屏、360×800 的中英文半屏及完整模式、1366×768 的中英文完整模式；覆盖大厅、目录、活动、赛事、商城和个人页。
- 每组均检查实际内容存在、无空全屏提示页、无横向溢出、内部链接不改变模式及英文正文无残留中文。
- 半屏活动实测：签到与任务领取后待领取数从 2 变为 0，按钮禁用；转盘次数从 3 变为 2，奖励记录正确显示三条本页新增记录。
- 半屏赛事实测：规则弹窗、报名确认可用；报名后按钮禁用。点击更多赛事只出现提示，仍留在半屏原页。
- 半屏个人页实测：资料、本周统计和资产实际展示，编辑及资产浮窗保留；更多账号内容不导航。
- 半屏充值实点进入 `store.html?mode=half` 并显示四档礼包；获得实点进入 `events.html?mode=half` 并显示可操作的活动内容。
- 首页带旧 `mode=half` 参数时仍默认选 full，无 checkbox，按钮立即可用；默认点击进入 full。主动选择 half 后进入 half，后续充值/获得仍保持 half。
- 删除了 `FullScreenPrompt` 页面拦截和直接进入 full 的业务链接。`localizedHref` 测试覆盖当前布局优先，独立首页不传入布局锁。

## 九位数显示规则

|        数值 | 半屏货币栏 | 全屏货币栏 | 浮窗与无障碍标签 |
| ----------: | ---------: | ---------: | ---------------: |
|       5,700 |       5.7K |      5,700 |            5,700 |
|      52,860 |      52.9K |     52,860 |           52,860 |
|   5,700,000 |       5.7M |       5.7M |        5,700,000 |
| 567,000,000 |       567M |       567M |      567,000,000 |
| 999,999,999 |         1B |         1B |      999,999,999 |

短格式最多一位小数，只改变展示，不改变钱包数值。半屏从千位、全屏从十万位开始缩写；个人页半屏资产卡同样用短格式，完整模式资产卡保留完整余额。

九位数已通过单元测试和公开宿主上下文的实际布局检查：320px 宽半屏中，567,000,000 与 999,999,999 在浮窗中完整显示，金额不溢出，获得/充值操作区可见。测试 HTML 不进入交付包。

## 保持不变的范围

- USD 礼包与折扣、昵称验证、游戏筛选、兑换码、转盘角度和宿主桥接测试继续通过。
- 大厅不添加缩放图标、货币加号或独立退出登录。
- 说明只放在独立原型首页；业务页展示实际内容，额外功能提示只在点击对应入口后出现。
- 游戏临时全屏是既有的游戏流程，不是半屏业务页面切换布局。
- 数据和结果仍供原型评审，不代表真实账户或服务端结算；宿主接口见 [HOST-INTEGRATION.md](HOST-INTEGRATION.md)。

## 发布复核

1. 执行 `npm run verify`，启动本项目的本地预览，确认地址实际显示 Joyloop。
2. 从首页进入 full/half，按上表检查内容与操作；半屏任何普通导航及获得/充值都应保留 half。
3. 在干净提交上运行 `npm run package:pages`。
4. 检查 ZIP 完整性、文件哈希与 manifest 的 `version/sourceCommit/dirty: false`。本轮未代为执行 Cloudflare 线上部署。
